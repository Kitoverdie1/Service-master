
export const metadata = {
    title: "ระบบงานแจ้งซ่อม - ตั้งค่า - ผู้ใช้งาน"
}

export default async function SettingsUsersLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
